export enum NavigationPlace {
  Root = 'root',
  Favorites = 'favorites',
  Latest = 'latest',
  Dashboards = 'dashboards',
  Datasets = 'datasets',
  Widgets = 'widgets',
  Connections = 'connections',
}
